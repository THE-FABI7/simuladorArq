/* La declaración `declare interface RecordData` declara una interfaz llamada `RecordData`. Una
interfaz en TypeScript es una forma de definir la estructura de un objeto. */
declare interface RecordData {
  addressType: string;
  value: number;
}

/* La `declaración de instrucción de interfaz` declara una interfaz llamada `Instrucción`. Esta
interfaz define la estructura de un objeto que tiene dos propiedades: `dirección` de tipo `cadena` y
`valor` de tipo `cadena`. */
declare interface Instruction {
  address: string;
  value: string;
}
